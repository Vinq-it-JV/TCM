{% extends 'base.html.twig' %}

{% block contents %}
    {% if app.user %}
        <div ng-controller="packetlogCtrl" ng-init="getPacketlog()">
            {% block header %}
                {% include 'AdministrationBundle:header:header.html.twig' %}
            {% endblock %}
            {% block page %}
                <div class="page">
                    <div class="container">
                        <div class="row">
                            <div class="panel-body">
                                <h4 ng-cloak>
                                    <i class="fa fa-book"></i> {{ ng.translate('CONFIGURATION.PACKET_LOG') }}
                                </h4>
                                <hr/>
                                <div class="well">
                                    <section st-table="logCollection" st-safe-src="log.logs()">
                                        <div class="row">
                                            <div class="col-md-3 col-xs-6">
                                                <input st-search class="form-control input-sm"
                                                       placeholder="{{ ng.translate('SEARCH') }}" type="text" ng-cloak/>
                                            </div>
                                        </div>
                                        <br/>
                                        <div class="row">
                                            <div class="col-md-12 col-xs-12">
                                                <table class="table table-striped table-bordered">
                                                    <thead>
                                                    <tr>
                                                        <th st-sort="Uid" class="st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.DEVICE_ID') }}</th>
                                                        <th class="col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.INPUTS') }}</th>
                                                        <th class="st-nosort st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.INTERNAL') }}</th>
                                                        <th class="st-nosort st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.OUTPUT_1') }}</th>
                                                        <th class="st-nosort st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.OUTPUT_2') }}</th>
                                                        <th class="st-nosort st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.OUTPUT_3') }}</th>
                                                        <th class="st-nosort st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.OUTPUT_4') }}</th>
                                                        <th class="st-nosort st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.OUTPUT_5') }}</th>
                                                        <th class="st-nosort st-sortable col-md-1"
                                                            ng-cloak>{{ ng.translate('LOG.OUTPUT_6') }}</th>
                                                        <th class="st-nosort col-md-1 text-right"
                                                            ng-cloak>{{ ng.translate('ACTION') }}</th>
                                                    </tr>
                                                    </thead>
                                                    <tbody>
                                                        {% verbatim %}
                                                        <tr ng-repeat="log in logCollection">
                                                            <td ng-cloak>{{ log.Uid }}</td>
                                                            <td ng-cloak>
                                                                <div class="label" ng-repeat="state in log.InputStates track by $index" ng-class="{'label-danger':!state, 'label-success':state}">
                                                                    <i class="fa fa-fw" ng-class="{'fa-check':state, 'fa-remove':!state}"></i>
                                                                </div>
                                                            </td>
                                                            <td ng-cloak>{{ log.Internal | number:2 }}</td>
                                                            <td ng-repeat="output in log.Outputs" class="text-right"ng-cloak>
                                                                <div>{{ output.uid }}</div>
                                                            </td>
                                                            <td ng-cloak></td>
                                                        </tr>
                                                        {% endverbatim %}
                                                    </tbody>
                                                </table>
                                            </div>
                                        </div>
                                    </section>
                                </div>
                                <div class="panel-footer">
                                    <div class="row">
                                        <div class="col-md-12">
                                            <button ng-click="reloadPage()" class="btn btn-sm btn-primary pull-left" ng-cloak>
                                                <i class="fa fa-refresh"></i> {{ ng.translate('REFRESH') }}
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            {% endblock %}
        </div>
    {% endif %}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('bundles/app/js/app/controllers/packetlogCtrl.js') }}"></script>
    <script src="{{ asset('bundles/app/js/app/datasets/logs.js') }}"></script>
{% endblock %}

